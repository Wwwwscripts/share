name: Discord Notify

on:
  push:
    branches: [ main ]  # změň pokud máš jinou branch

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send message to Discord
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
            -X POST \
            -d "{\"content\":\"💬 ${{ github.event.head_commit.message }}\"}" \
            "$WEBHOOK"

name: Discord Notify

on:
  push:
    branches: [ main ]  # ujisti se, že commit jde na main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Test Discord webhook
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
            -X POST \
            -d '{"content":"💬 Test zpráva z GitHub Actions"}' \
            "$WEBHOOK"

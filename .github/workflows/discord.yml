name: Discord Notify

on:
  push:
    branches: ["*"]  # všechny branche

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Send last commit message to Discord
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          # vezmeme poslední commit message z aktuálního repa
          MESSAGE=$(git log -1 --pretty=%B)

          # escapování uvozovek pro JSON
          MESSAGE=$(echo "$MESSAGE" | sed 's/"/\\"/g')

          # pošleme zprávu do Discordu
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\":\"💬 $MESSAGE\"}" \
               "$WEBHOOK"
